

        <div class="row">
            <div class="col-lg-10 mx-auto">
                    <div class="d-flex justify-content-center align-items-center"  style="height: 100vh;">
                    <div class="col-lg-5 "  style="background: rgb(223, 223, 223);" >
                        <div 
                            class="login_wrap widget-taber-content p-30 background-white border-radius-10 mb-sm-5 ">
                              <div class="padding_eight_all" style="background: rgb(250, 249, 249);"></div>
                                <div class="heading_s1">
                                    <h3 class="mb-30" style="display: flex; justify-content: center; align-items: center;">Login</h3>
                                    <p id="errorEmail" class="text-danger"></p>
                                </div>
                                <form id="loginPage">
                                    <div class="form-group">
                                        <input type="text" id="email" required="" name="email" placeholder="Your Email">
                                        <p id="error1" class="text-danger"></p>
                                    </div>
                                    <div class="form-group">
                                        <input required="" id="password" type="password" name="password"
                                            placeholder="Password">
                                        <p id="error2" class="text-danger"></p>
                                    </div>
                                    <div class="login_footer form-group">
                                        <div class="chek-form">
                                            <div class="custome-checkbox">
                                                <input class="form-check-input" type="checkbox" name="checkbox"
                                                    id="exampleCheckbox1" value="">
                                                <label class="form-check-label" for="exampleCheckbox1"><span>Remember
                                                        me</span></label>
                                            </div>
                                        </div>
                                        <!-- <a class="text-muted" href="#">Forgot password?</a> -->
                                    </div>
                                    <div>
                                        <a class="text-muted pb-3" style="display: flex; justify-content: center; align-items: center;" href="/otppage">Login with OTP</a>
                                    </div>
                                    <div class="form-group " style="display: flex; justify-content: center; align-items: center;">
                                        <!-- <button type="submit" class="btn btn-fill-out btn-block hover-up" name="login">Log in</button> -->
                                        <button type="button" onclick="validate()"
                                            class="btn btn-fill-out btn-block hover-up" name="login">Log in</button>

                                    </div>
                                </form>
                              
                                <a href="/signup" style="display: flex; justify-content: center; align-items: center;">
                                    <button type="submit" class="btn btn-fill-out hover-up" name="login">Create Account</button>
                                </a>
                                
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</section>




<!-- Vendor JS-->

</body>


<script>
    function validate() {
        const email = document.getElementById('email')
        const password = document.getElementById('password')

        const emailValue = email.value.trim()
        const passwordValue = password.value.trim()

        const error1 = email.parentElement.querySelector('p')
        const error2 = password.parentElement.querySelector('p')

        error1.innerText = ""
        error2.innerText = ""

        let flag = 1
        if (emailValue == "") {
            error1.innerText = "field is required"
            flag = 2

        }

        if (passwordValue == "") {
            error2.innerText = "field is requied"
            flag = 2
        }

        if (flag == 1) {
            loginFunc(emailValue, passwordValue)
        }

    }

    function loginFunc(email, password) {
        
        $.ajax({
            url: '/login',
            method: 'post',
            data: $('#loginPage').serialize(),
            success: (response) => {

              
                if (response.emailStatus === false) {
                   
                    document.getElementById('errorEmail').innerText = 'email doesnt exist';
                } else if (response === false ) {
                  
                    document.getElementById('errorEmail').innerText = 'You have been blocked by the admin';
                } else {
                    if(
                        response.users
                    )
                    location.href = '/shop';
                }


            }
        })
    }

</script>